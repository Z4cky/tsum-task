export interface IUser {
  FIO: string;
  // surname: string;
  // fatherName?: string;
  gender: string;
  dateOfBirth: Date;
  family?: string;
  kids?: number;
  email: string;
  comment?: string;
}
